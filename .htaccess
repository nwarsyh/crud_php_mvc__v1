Options -Indexes
Options -MultiViews
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /crud_php_mvc__v1/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?url=$1 [L]
</IfModule>
